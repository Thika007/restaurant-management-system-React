# Server Configuration
PORT=5000

# Database Configuration
# Use IP address for remote database (e.g., 192.168.1.100)
# Use localhost for local database
DB_SERVER=localhost
DB_PORT=1433
DB_DATABASE=BakeryManagementDB
DB_USER=sa
DB_PASSWORD=your_password
# Leave empty for default instance, or use instance name like SQLEXPRESS
DB_INSTANCE=

# JWT Secret (change this in production!)
JWT_SECRET=your-secret-key-change-this-in-production
